<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <title>create a game</title>
    <%- include('./styles') %> 
</head>
<body>
    <div class="container-fluid bg-dark bg-gradient text-white">
        <h3 class="py-5 text-center">update <%= game.title %></h3>
    </div>

    <div class="container mt-5 bg-white shadow-lg rounded p-5">
        <form method="POST" action="/catalog/game/<%= game._id %>/update">
            <div class="mb-3">
                <label for="title">Game title</label>
                <input type="text" name="title" class="form-control" value="<%= game.title %>">
            </div>
            <div class="mb-3">
                <label for="summary">Game summary</label>
                <input type="text" name="summary" class="form-control" value="<%= game.summary %>">
            </div>
            <% genres.forEach( genre => {%>
                <% let genreChk = genre.checked ? 'checked' : '' %>
                <input type="checkbox" name="genre" id="<%=genre._id%>" value="<%=genre._id%>" <%=genreChk%> >
                <label for="<%=genre._id%>"><%=genre.name%></label>
              <%})%>
            <br>

            <button type="submit" class="btn btn-dark mt-5">Submit</button>
        </form>

    </div>
</body>
</html>